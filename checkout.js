export default function handler(req, res){
  // Placeholder: collegheremo Stripe più avanti.
  // Una volta pronte le chiavi, sostituisci questo handler con la versione Stripe.
  return res.status(200).json({ msg: "Checkout non ancora attivo. Collegheremo Stripe dopo." });
}
